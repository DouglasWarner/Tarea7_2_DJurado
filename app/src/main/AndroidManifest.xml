<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.inventory">

    <application
        android:name=".iu.InventoryApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.example.inventory.iu.InventoryActivity">

        </activity>
        <activity
            android:name="com.example.inventory.iu.signup.SignUpActivity"
            android:theme="@style/noActionBar" />
        <activity
            android:name="com.example.inventory.iu.login.LoginActivity"
            android:theme="@style/noActionBar" />
        <activity
            android:name="com.example.inventory.iu.SplashActivity"
            android:theme="@style/noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Incluir fuentes -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>